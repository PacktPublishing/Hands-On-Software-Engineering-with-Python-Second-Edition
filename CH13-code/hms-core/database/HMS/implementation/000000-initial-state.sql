/**
  * This file should represent the current state of the
  * database after any "squashing" operations have been
  * executed (see the README file).
  *
  * As of the start of the project, all it does is execute
  * a SHOW TABLES to *show* that state.
  */

SHOW TABLES;
