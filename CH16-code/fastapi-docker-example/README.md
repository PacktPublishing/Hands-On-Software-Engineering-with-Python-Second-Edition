# Makefile usage

| Command        | Purpose                 |
| -------------- | ----------------------- |
| `make build`   | builds the image        |
| `make run`     | runs the container      |
| `make rebuild` | rebuilds and runs fresh |
